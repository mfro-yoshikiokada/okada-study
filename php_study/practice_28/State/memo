　stateという単語は「状態」の意味がありますが、Stateパターンは物ではなく「状態」をクラスとして表現し、「状態」ごとに振る舞いを切り替えられるようにするパターンです。

構成要素
Stateクラス
それぞれの状態に共通のAPIを定義するクラス。
状態固有の振る舞いを行うメソッドを定義する。
ConcreteStateクラス
Stateクラスのサブクラス。
状態ごとの具体的な処理内容を記述する。
Contextクラス
State型のオブジェクトを内部に保持し、具体的な処理をそのオブジェクトに委譲するクラス。
